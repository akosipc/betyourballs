ul.navbar
  = if @conn.assigns.current_user do
    li= link "Log Out", to: session_path(@conn, :delete), method: :delete
    li= link "Matches", to: match_path(@conn, :index)
  - else
    li= link "Register", to: registration_path(@conn, :new)
    li= link "Sign In", to: session_path(@conn, :new)
